package com.opple.fa.myTasks.service.impl;

import com.opple.fa.myTasks.dao.MyTasksDAO;
import com.opple.fa.myTasks.entity.MyTasks;
import com.opple.fa.myTasks.service.MyTasksService;
import com.opple.util.Pager;
import lombok.Getter;
import lombok.Setter;

import java.util.List;

/**
 * Created by zhangshulong on 2017/2/28.
 */
public class MyTasksServiceImpl implements MyTasksService {
    @Setter
    @Getter
    private MyTasksDAO myTasksDAO;

    @Override
    public Pager<MyTasks> searchMyTasksList(Pager<MyTasks> pager, MyTasks myTasks) {
        List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList(pager, myTasks);
        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts(myTasks);
        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
    }

    @Override
    public Pager<MyTasks> searchMyTasksList1(Pager<MyTasks> pager, MyTasks myTasks) {
        List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList1(pager, myTasks);
        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts1(myTasks);
        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
    }

    @Override
    public Pager<MyTasks> searchMyTasksList2(Pager<MyTasks> pager, MyTasks myTasks) {
        List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList2(pager, myTasks);
        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts2(myTasks);
        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
    }

    @Override
    public Pager<MyTasks> searchMyTasksList3(Pager<MyTasks> pager, MyTasks myTasks) {
        List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList3(pager, myTasks);
        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts3(myTasks);
        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
    }

    @Override
    public Pager<MyTasks> searchMyTasksList4(Pager<MyTasks> pager, MyTasks myTasks) {
        List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList4(pager, myTasks);
        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts4(myTasks);
        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
    }

    @Override
    public Pager<MyTasks> searchMyTasksList5(Pager<MyTasks> pager, MyTasks myTasks) {
        List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList5(pager, myTasks);
        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts5(myTasks);
        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
    }

   @Override
    public Pager<MyTasks> searchMyTasksList6(Pager<MyTasks> pager, MyTasks myTasks) {
        List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList6(pager, myTasks);
        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts6(myTasks);
        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
    }

    @Override
    public Pager<MyTasks> searchMyTasksList7(Pager<MyTasks> pager, MyTasks myTasks) {
        List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList7(pager, myTasks);
        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts7(myTasks);
        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
    }

    @Override
    public Pager<MyTasks> searchMyTasksList8(Pager<MyTasks> pager, MyTasks myTasks) {
        List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList8(pager, myTasks);
        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts8(myTasks);
        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
    }

	@Override
	public Pager<MyTasks> searchMyTasksList9(Pager<MyTasks> pager,
			MyTasks myTasks) {
		   List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList9(pager, myTasks);
		   Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts9(myTasks);
		   return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
	}

	@Override
	public Pager<MyTasks> searchMyTasksList10(Pager<MyTasks> pager,
			MyTasks myTasks) {
		 	List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList10(pager, myTasks);
		   Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts10(myTasks);
		   return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
	}

	@Override
	public Pager<MyTasks> searchMyTasksList11(Pager<MyTasks> pager,
			MyTasks myTasks) {
			List<MyTasks> auditStatisticsList = myTasksDAO.searchMyTasksList11(pager, myTasks);
	        Long auditStatisticsCount = myTasksDAO.searchMyTasksListCounts11(myTasks);
	        return Pager.cloneFromPager(pager, auditStatisticsCount, auditStatisticsList);
	}
}
